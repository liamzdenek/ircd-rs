[package]
name = "net"
version = "0.0.1"
authors = []

[lib]
name = "net"
path = "lib.rs"

[dependencies]
net_traits = { path = "../net_traits" }
user = { path = "../user" }
user_traits = { path = "../user_traits" }
